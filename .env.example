# Demo mode: mock data, no API calls (set to true to preview without keys)
VITE_DEMO_MODE=false

# RAWG API key (games) — https://rawg.io/apidocs — free tier, account required
VITE_RAWG_API_KEY=

# Comics FR : Urban Comics via proxy Vite /proxy/urban-comics (dev uniquement)

# Optional CORS proxy pour RAWG — pas utilisé pour le scraping HTML
# VITE_CORS_PROXY=
