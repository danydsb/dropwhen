# Demo mode: mock data, no API calls (set to true to preview without keys)
VITE_DEMO_MODE=false

# RAWG API key (games) — https://rawg.io/apidocs
# Sur Vercel : Project Settings → Environment Variables
VITE_RAWG_API_KEY=

# Comics FR : proxy /api/proxy/urban-comics (Edge Function Vercel en prod, Vite en dev)

# Optional CORS proxy pour RAWG si appels directs bloqués
# VITE_CORS_PROXY=
